# Known Issues

No known active issues in the initialized template state.

## Deferred Observations

- Validation commands are placeholders because this template does not prescribe a runtime.
- Graphify outputs are not generated or committed by default.
- Template users must replace generic project memory with project-specific context after creating a new repository.
